1. Always run the build and lint with yarn to check the build and fix any errors after you make changes to the code.

2. After all errors are fixed, automatically commit and push the changes to the repository.
